# Deterministic fixture repository

This tiny repository is copied into an isolated temporary Git checkout by the
Architecture 022 suite. Its tracked bytes are source-of-truth input. The suite
never reads an operator home directory or uses this repository as a network or
dependency source.
