services:
  embedding-service:
    ports:
      - "127.0.0.1:${REVOLVR_EMBEDDING_PORT:-8080}:8080"

  postgres:
    ports:
      - "127.0.0.1:${REVOLVR_POSTGRES_PORT:-5432}:5432"
    networks:
      - development

networks:
  development:
